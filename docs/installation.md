# Installation Guide 🚀

To install `torch-pharma`, use pip:

```bash
pip install torch-pharma
```

Alternatively, install from source:

```bash
git clone https://github.com/TomiTsuma/torch_pharma.git
cd torch_pharma
pip install -e .
```
